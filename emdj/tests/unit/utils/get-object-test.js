import getObject from '../../../utils/get-object';
import { module, test } from 'qunit';

module('Unit | Utility | get object');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = getObject();
  assert.ok(result);
});
